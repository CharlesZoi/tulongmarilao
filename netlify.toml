[build]
  command = "npm run build"
  publish = "."

[build.environment]
  NODE_VERSION = "18"
  # Disable Netlify's secret detection
  NETLIFY_SECRET_SCAN_DISABLED = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404
