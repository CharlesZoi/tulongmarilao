[build]
  command = "npm run build"
  publish = "."

[build.environment]
  NODE_VERSION = "18"
  # Disable Netlify's secret detection temporarily
  NETLIFY_BUILD_SECRET_SCAN = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404
